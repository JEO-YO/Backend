package com.unittest.userinfosave.model.user.service;

import com.unittest.userinfosave.model.user.User;

public interface UserService {

    void join(User user);

    void find(String activityArea);
}
